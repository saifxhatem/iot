<!DOCTYPE html>
<html lang="en">
<head>
	<% include head %>
</head>
<body class="container">
<header>
	<% include header %>
</header>

<main>
    <div class="jumbotron">
        <h2>Home</h2>
        

    <p id="hello">Welcome <% if(locals.sess.req.session.user_id){ %> <%= sess.req.session.user_name %> <% }else{ %>Guest<% } %></p>

    </div>
</main>

<footer>
	<% include footer %>
</footer>

</body>
</html>